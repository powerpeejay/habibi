# DIRECTION [LETTER] — [Direction Name]

> Phase 1 artifact. One of three. Reviewed alongside `previews/[letter].html`.
> See `directions/` siblings for the other two options.

---

## Risk profile

`safe` / `adjacent` / `bold` — pick one.

## One-sentence philosophy

[E.g. "A regional Sunday-paper feel — handcrafted typography, generous whitespace, photography that smells like the bakery floor."]

## Why this fits this client

Cite `INTAKE.md` directly:
- Audience match: [reference §2 of INTAKE]
- Primary goal alignment: [how this direction serves §3]
- Old-site improvement: [what §4 "fails" this fixes]

## The ONE move

The single visual decision that makes this direction memorable. If a stranger described the site in five words, this is what they'd describe.

[E.g. "Display headlines set in Migra at 9rem, breaking the grid by 40px to the left, with grain overlay at 4% opacity over hero photography."]

## Color tokens

| Token | Hex | Role |
|---|---|---|
| `--color-bg` | `#______` | Base canvas |
| `--color-surface` | `#______` | Elevated surface |
| `--color-surface-2` | `#______` | Secondary surface |
| `--color-border` | `#______` | Hairlines |
| `--color-text` | `#______` | Primary text |
| `--color-text-muted` | `#______` | Secondary text |
| `--color-accent` | `#______` | CTA, highlights |
| `--color-accent-hover` | `#______` | Accent hover |

**Color logic:** [what's dominant, where accent appears, what's forbidden]

## Typography

- **Display:** [Family — source — weight(s) used]
- **Body:** [Family — source — weight(s) used]
- **Self-hosted:** [ ] confirmed (no Google Fonts CDN)
- **Sample headline rendering:** [a real client headline rendered in this pairing — note tracking, line-height, size]

## Spacing & layout character

- **Density:** generous / balanced / controlled
- **Asymmetry:** [yes/no, where]
- **Section rhythm:** [e.g. "alternating full-bleed photography sections with text-only sections at narrower max-width"]

## Texture / atmosphere

- [grain, noise, gradient mesh, layered transparency, flat — pick and describe]
- [if photography: art-direction notes — e.g. "warm tungsten, shallow DOF, hands-and-craft framing, never empty interiors"]

## Mood references (real sites)

Three URLs that share the *feel*, not the *layout*. Pull from `INSPIRATION.md` (Personal Vault first, then per-direction starters). Name what's borrowed from each — vague "I like this" doesn't qualify.

1. [URL] — [what we're taking, e.g. "the way the navigation dissolves on scroll"]
2. [URL] — [e.g. "the editorial pull-quote treatment in long-form sections"]
3. [URL] — [e.g. "the muted accent applied only to action verbs"]

If any reference is from a use-with-caution source (Dribbble / Awwwards / Behance), state explicitly what's discarded alongside what's taken.

## Trade-offs

What this direction sacrifices vs. the other two:

- [e.g. "Less immediate trust signaling than B — better suited to a brand the client wants to *evolve* into, not the one that already exists."]
- [e.g. "Heavier on photography — needs strong source material; will fail on stock."]

## Implementation notes

- **Best-fit components:** [e.g. "asymmetric hero, magazine-style 2-up grids, full-bleed photography breaks"]
- **Avoid for this direction:** [e.g. "card grids, icon rows, gradient backgrounds"]
- **Performance considerations:** [e.g. "grain overlay must be SVG, not raster; max ~3KB"]
